/**
 * Type declarations for globals injected by PHP via wp_localize_script.
 */

declare global {
	interface Window {
		by40qGlobalSettings: {
			restUrl: string;
			nonce: string;
		};
	}
}

export {};
