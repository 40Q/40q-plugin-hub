{
  "name": "by40q-plugin-hub",
  "version": "1.0.0",
  "description": "40Q Agency – WordPress Plugin Workspace",
  "private": true,
  "workspaces": [
    "plugins/by40q-core",
    "plugins/by40q-global-settings"
  ],
  "scripts": {
    "env:start": "wp-env start",
    "env:stop": "wp-env stop",
    "env:restart": "wp-env start --update",
    "env:reset": "wp-env clean all",
    "env:destroy": "wp-env destroy",
    "env:logs": "wp-env logs",
    "cli": "wp-env run cli --",
    "lint": "composer run lint",
    "lint:fix": "composer run lint:fix"
  },
  "devDependencies": {
    "@wordpress/env": "^10.0.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "yarn": ">=1.22.0"
  }
}
